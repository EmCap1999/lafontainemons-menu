{
  "version": "1.0.0",
  "scripts": {
    "lint": "biome check .",
    "lint:ci": "biome ci .",
    "lint:fix": "biome check --write --unsafe .",
    "typecheck:all": "npm run typecheck --workspace=backend && npm run typecheck --workspace=frontend && npm run typecheck --workspace=database",
    "update": "npm run update:check && npm run update:install && npm run update:audit",
    "update:check": "npm outdated --workspaces || true",
    "update:install": "npm update --workspaces && npm update",
    "update:audit": "npm audit --audit-level=moderate --workspaces",
    "update:force": "npm run update:install && npm run update:audit -- --fix"
  },
  "workspaces": [
    "backend",
    "frontend",
    "database"
  ],
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "@types/node": "^24.10.0",
    "dotenv": "^17.2.2",
    "dotenv-cli": "^11.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "esbuild": "^0.27.1"
  }
}
