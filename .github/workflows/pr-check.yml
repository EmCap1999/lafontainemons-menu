name: 🛡️ PR Check
on:
  pull_request:
    branches: [main, dev]

jobs:
  lint-and-build:
    name: 🔍 Lint & Build
    runs-on: ubuntu-24.04
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🧼 Auto-fix lint issues
        run: npm run lint:fix

      - name: 📋 Check for remaining lint issues
        run: npm run lint

      - name: 🏗️ Build all projects
        run: |
          # Create stub environment if needed
          mkdir -p frontend/src/environments
          if [ ! -f frontend/src/environments/environment.ts ]; then
            cat > frontend/src/environments/environment.ts << 'EOF'
          export const environment = {
            production: false,
            apiUrl: 'http://localhost:3000',
            frontendUrl: 'http://localhost:4200'
          };
          EOF
          fi
          
          # Build frontend
          cd frontend && npm run build && cd ..
          
          # Test backend startup
          cd backend && timeout 10s npm start || echo "✅ Backend starts correctly" && cd ..
          
          # Validate Drizzle config
          cd db && node -e "
            import('./drizzle.config.js').then(config => {
              console.log('✅ Drizzle config valid');
            }).catch(err => {
              console.error('❌ Drizzle config error:', err.message);
              process.exit(1);
            });
          " && cd ..